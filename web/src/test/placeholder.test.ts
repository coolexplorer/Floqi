// Placeholder test — actual feature tests will be added in Sprint 1 (TDD)
describe("Web app setup", () => {
  it("should have vitest configured correctly", () => {
    expect(true).toBe(true);
  });
});
